Replace these clips with 3–8 seconds of **native** speech for the matching accent and gender.

`python download_refs.py` fills in public examples when URLs work. The UK examples from Seed-VC may not match gender or accent well — swap them for VCTK / native British clips if conversion sounds wrong.

